import React, { Component } from 'react';

class AES_Matrix extends Component {
    render() {
        return (
            <div>
                <h1>{this.props.encrypt}</h1>
            </div>
        )
    }
}

export default AES_Matrix;
